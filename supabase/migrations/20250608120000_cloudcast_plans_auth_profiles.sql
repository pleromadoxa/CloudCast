-- Applied to CloudCast Supabase project (ixjydnkpnyxnckhkqhue)
-- Migration: cloudcast_plans_auth_profiles

-- See Supabase dashboard for full SQL applied via MCP.
-- Adds: subscription_plans, profiles, plan-aware mixer_sessions,
-- device_type on paired_devices, updated RPCs for auth + plan limits.
