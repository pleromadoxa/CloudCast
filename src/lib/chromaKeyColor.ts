/** Standard chroma key green — shared by Video Mixer, Regal Display KEY mode, and templates. */
export const CHROMA_KEY_GREEN = '#00ff00';

export function chromaKeyGreenRgb(): [number, number, number] {
  return [0, 255, 0];
}
