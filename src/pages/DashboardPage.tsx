/**
 * Dashboard UI is rendered by ProductionHost so the mixer session survives
 * in-app navigation while ON AIR. This route only enforces authentication.
 */
export function DashboardPage() {
  return null;
}
